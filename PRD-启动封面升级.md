# PRD — 启动封面升级：心跳萤火 · 序章

---

## 1. 背景与目标

### 1.1 当前状态
启动页面由静态渐变背景 + 标题 + 手势提示 + 一个按钮组成，缺乏动态韵律和情感张力。

### 1.2 目标
将启动封面升级为一个有叙事感的「序章」场景，让用户在点击「开启」之前，已经被带入浪漫氛围。

### 1.3 设计策略
混合 **B 心跳序章** × **C 萤火初遇**，辅以 A 的文字质感和 D 的克制背景流动：

| 元素 | 来源 | 说明 |
|------|------|------|
| 中央心跳粒子环 + 呼吸动画 | B 心跳序章 | 作为视觉焦点，建立亲密的生命力 |
| 萤火粒子从底部升腾环绕 | C 萤火初遇 | 提供温暖和梦幻感 |
| 标题手写体 + 金光微光 | A 星辰手札 | 文字质感和仪式感 |
| 背景微妙暗色渐变流动 | D 极光序幕（克制版） | 底层氛围，不抢戏 |
| 按钮随心跳微微发光 | B 心跳序章 | 引导点击，与心跳节奏同步 |

---

## 2. 功能详情

### 2.1 心跳粒子环 (Heartbeat Ring)

**视觉描述：** 在封面中央，由 200 个玫瑰金发光粒子构成的圆环（直径约 80px），以约 72 BPM 的节奏缓慢呼吸缩放。每次「心跳」膨胀约 8%，并发出一圈极淡的径向光晕。

**技术要点：**
- 类型：CSS 动画 / Canvas 2D / 简易 WebGL（视复杂度）
- 粒子数：~200
- 颜色：玫瑰金 `#e8a0b4` → 淡粉 `#ffb6c1`
- 动画：周期约 0.83s（72 BPM），缩放曲线使用缓入缓出
- 光晕：径向渐变，随心跳 opacity 在 0 → 0.3 之间波动
- 位置：标题文字上方，视觉焦点

### 2.2 萤火粒子 (Firefly Particles)

**视觉描述：** 300-500 颗暖金色/淡粉色粒子从画面底部缓缓升腾，像夏夜萤火虫。粒子有大小和速度差异，较大的萤火会短暂停留后继续上升，部分粒子会被心跳环吸引，在环周围形成环绕轨迹。

**技术要点：**
- 类型：Canvas 2D 或 CSS animation（大规模粒子建议 Canvas）
- 粒子数：300-500
- 颜色：暖琥珀 `#ffd700` / 淡粉 `#ffb6c1` / 暖白 `#fff5e6`，随机分布
- 运动：底部（y=100%）→ 升腾 → 顶部消散，速度 0.3-1.5 px/frame
- 环绕：部分萤火在接近心跳环时被引力捕获，做短弧线绕行
- 闪烁：每个粒子有独立的 opacity 正弦波动，模拟萤火虫发光
- 大小：0.8-3.5px，近大远小的模糊光斑效果

### 2.3 标题文字

**视觉描述：** 手写衬线字体「Miracle Heart」，带有微弱的金色辉光和文字阴影，仿佛被星光书写而成。副标题使用无衬线小字，字母间距拉开。

**技术要点：**
- 字体：`'Great Vibes', cursive`（已有）
- 颜色：暖白 → 淡金渐变
- 辉光：`text-shadow: 0 0 20px rgba(255, 182, 193, 0.5), 0 0 60px rgba(255, 215, 0, 0.2)`
- 动画：页面加载时 opacity 0→1，轻微 Y 轴上升（translateY -8px），持续 1.2s

### 2.4 背景渐变流动

**视觉描述：** 暗色背景上有极其克制的色相漂移——从深蓝紫到暗玫瑰色调，完整周期约 30 秒。变化幅度很小，提供底层氛围但不分散注意力。

**技术要点：**
- 类型：CSS `radial-gradient` + `@keyframes` 色相缓慢旋转
- 周期：约 30 秒一个完整循环
- 幅度：色相偏移 ±15°，亮度变化极小
- 位置：`body` 或 `#start-screen` 背景

### 2.5 开启按钮

**视觉描述：** 圆角胶囊按钮，与心跳环同步微微呼吸发光。悬停时辉光增强，点击时缩小。

**技术要点：**
- 动画：与心跳同频的 `box-shadow` 呼吸（周期 0.83s）
- 悬停：辉光半径增大 40%，`translateY(-2px)`
- 点击：`scale(0.96)` + 过渡 0.15s

---

## 3. 与现有系统的协调

| 现有元素 | 处理方式 |
|----------|----------|
| `#start-screen` 背景 | 替换为流动渐变背景 |
| `#start-title` | 保留，增强辉光效果 |
| `#start-sub` | 保留，微调间距和透明度 |
| `#start-hint` | 保留，降低 opacity（不干扰视觉焦点） |
| `#btn-start` | 保留，改为心跳呼吸动画 |
| 伪元素星空 `::before` | 保留，降低 opacity，与萤火粒子共存 |

---

## 4. 实现方案选择

有两种技术路线：

### 路线 1：纯 CSS + 少量 Canvas（推荐）

- 心跳环：CSS `border` + `box-shadow` + `@keyframes` 呼吸
- 萤火粒子：单个 `<canvas>` 覆盖在封面层，JS 驱动粒子运动
- 背景流动：CSS `@keyframes` 渐变
- 标题/按钮：CSS 动画

**优点：** 简单、性能好、不依赖 Three.js

### 路线 2：复用 Three.js

- 在启动封面嵌入一个小型 Three.js 场景
- 心跳环用 `THREE.Points` 环形粒子
- 萤火同样用粒子系统

**优点：** 与主场景视觉语言一致；**缺点：** 加载重量大，首次启动延迟

### 建议

使用 **路线 1（纯 CSS + Canvas）**。封面应该在 Three.js 加载完成之前就能看到，不需要等 WebGL 初始化。Canvas 2D 粒子性能足够，且启动更快。

---

## 5. 文件变更

| 文件 | 内容 |
|------|------|
| `index.html` | 新增 `<canvas id="firefly-canvas">` 在 start-screen 内 |
| `css/style.css` | 新增心跳环、萤火、背景流动、按钮呼吸等动画样式 |
| `js/cover.js` (新建) | 萤火粒子 Canvas 逻辑 + 心跳环引力场 |

---

## 6. 验收标准

- [ ] **AC-1:** 封面加载后心跳环以约 72 BPM 节奏呼吸，视觉明显但不刺眼
- [ ] **AC-2:** 萤火粒子从底部持续升腾，有大小/速度/闪烁差异
- [ ] **AC-3:** 背景色相缓慢漂移，变化克制不抢眼
- [ ] **AC-4:** 标题文字有金色辉光，加载时有淡入动画
- [ ] **AC-5:** 按钮与心跳同步呼吸发光
- [ ] **AC-6:** 点击按钮后封面平滑淡出，过渡自然
- [ ] **AC-7:** 移动端性能正常，萤火粒子不卡顿

---

## 7. 附录：视觉参考关键词

- 心跳环 → "pulsing ring heartbeat glow"
- 萤火粒子 → "firefly particle system canvas", "floating warm particles"
- 背景流动 → "slow gradient hue shift dark background"
- 标题辉光 → "gold text glow css"

---

*文档版本: v1.0 · 创建日期: 2026-06-11 · 状态: 待实现*
